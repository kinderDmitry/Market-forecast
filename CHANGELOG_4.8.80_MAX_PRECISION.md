# Market Forecast PRO X 4.8.80

- Усилен admission-gate прогнозов: дополнительная проверка исторического edge на горизонтах 3/8/16 свечей.
- Направление проходит только при положительной поддержке минимум 2 из доступных горизонтов и минимальном edge не ниже 48%.
- В объяснение прогноза добавлена прозрачная статистика мультигоризонтной проверки.
- Верхние вкладки/фильтр в разделе «Отслеживание» остаются обычным элементом LazyColumn и прокручиваются вместе с содержимым; stickyHeader не используется.
- Устранён риск `Unresolved reference abs` в ScannerForegroundService: для сортировки используется явный `kotlin.math.abs`.
- Версия 4.8.80, versionCode 119.
