# Market Forecast PRO X 4.8.81

- Scanner controls are now part of the same vertically scrollable content as Tracking, so the scope/type/timeframe filters do not remain pinned to the top while results are scrolled.
- Scanner progress, status, delete-all action and results live in one scroll container.
- Preserved scanner foreground-service behavior, selected/all-market modes, type filters and timeframe filters.
- Replaced the scanner sort expression using `abs()` with an equivalent allocation-free expression to avoid Kotlin compiler resolution issues.
- Version: 4.8.81 / versionCode 120.
